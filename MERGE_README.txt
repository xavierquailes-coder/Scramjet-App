SquiddCoreOS + Scramjet same-origin Render build

Root / serves SquiddCoreOS.
/browser/ serves the Scramjet browser.
/wisp/ is handled by the Node server.

Render settings:
Build command: npm install (or pnpm install)
Start command: npm start

Deploy the contents of this folder as one Render Web Service.
